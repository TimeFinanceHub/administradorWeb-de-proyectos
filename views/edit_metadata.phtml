<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RG012 - Dashboard</title>
    
    <link rel="icon" type="image/png" href="img-logo/favicon-96x96.png" sizes="96x96" />
<link rel="icon" type="image/svg+xml" href="img-logo/favicon.svg" />
<link rel="shortcut icon" href="/favicon.ico" />
<link rel="apple-touch-icon" sizes="180x180" href="img-logo/apple-touch-icon.png" />
<link rel="manifest" href="img-logo/site.webmanifest" />

    <style>
        :root {
            --primary-color: #2c3e50;
            --accent-color: #27ae60;
            --bg-color: #f4f4f9;
            --text-color: #333;
            --white: #ffffff;
        }

        body { 
            font-family: 'Courier New', monospace; 
            background: var(--bg-color); 
            color: var(--text-color); 
            margin: 0;
            padding: 20px;
            line-height: 1.6;
        }

        .container { 
            width: 100%;
            max-width: 900px;
            margin: 0 auto; 
            background: var(--white); 
            padding: 40px; 
            border-radius: 12px; 
            box-shadow: 0 10px 25px rgba(0,0,0,0.05); 
            box-sizing: border-box;
        }

        header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #eee;
            padding-bottom: 20px;
        }

        .brand-logo {
            max-width: 120px;
            height: auto;
            display: block;
            margin: 0 auto 15px auto;
        }

        h1 { 
            font-size: 1.5rem; 
            margin: 0; 
            color: var(--primary-color);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        input, textarea, button { 
            width: 100%; 
            padding: 15px; 
            margin: 10px 0; 
            box-sizing: border-box; 
            border: 1px solid #ddd;
            border-radius: 6px;
            font-family: inherit;
            font-size: 1rem;
        }

        button { 
            background: var(--primary-color); 
            color: var(--white); 
            border: none; 
            cursor: pointer; 
            font-weight: bold;
            transition: background 0.3s;
        }

        button:hover { background: #34495e; }

        /* BLOCKCHAIN UI */
        .block { 
            background: #ecf0f1; 
            border-left: 5px solid var(--accent-color); 
            padding: 20px; 
            margin-bottom: 20px; 
            word-break: break-all;
            border-radius: 4px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }

        .meta-info {
            font-size: 0.85rem;
            color: #555;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            border-bottom: 1px solid #ddd;
            padding-bottom: 5px;
        }
        
        .hash-container { 
            font-size: 0.75em; 
            color: #7f8c8d; 
            background: #eef;
            padding: 10px;
            border-radius: 4px;
            margin-top: 10px;
        }

        .alert { color: #e74c3c; text-align: center; background: #fadbd8; padding: 10px; border-radius: 4px; }
        .integrity-ok { color: var(--accent-color); font-weight: bold; text-align: center; margin: 20px 0; }
        .integrity-bad { color: #c0392b; font-weight: bold; text-align: center; margin: 20px 0; }
        
        .user-header {
            display: flex; 
            justify-content: space-between; 
            align-items: center;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }
        .logout-link {
             color:#e74c3c; text-decoration:none; border: 1px solid #e74c3c; padding: 5px 10px; border-radius: 4px;
        }

        /* MEDIA QUERIES */
        @media (max-width: 480px) {
            body { padding: 10px; }
            .container { padding: 15px; width: 100%; box-shadow: none; }
            h1 { font-size: 1.2rem; }
            .brand-logo { max-width: 100px; }
            .user-header {
                flex-direction: column;
                text-align: center;
                gap: 10px;
            }
        }
    </style>
</head>
<body>

<div class="container">
<header>
    <img src="rg012-logo.png" alt="Logo RG012" class="brand-logo">
    <h1>Editar Metadatos del Proyecto</h1>
</header>

<?php if (!empty($message)): ?><p class="alert"><?= htmlspecialchars($message) ?></p><?php endif; ?>

<div style="max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #eee; border-radius: 8px;">
    
    <h2>Proyecto #<?= $block['block_index'] ?></h2>
    
    <div style="background: #f0f0f0; padding: 15px; margin-bottom: 20px; border-radius: 4px;">
        <p><strong>Hash Criptográfico Original:</strong> <span style="font-size: 0.9em; word-break: break-all;"><?= $block['hash'] ?></span></p>
        <p><strong>Contenido Inmutable (Data):</strong></p>
        <textarea rows="4" disabled style="background:#ddd; cursor: not-allowed;"><?= htmlspecialchars($block['data']) ?></textarea>
    </div>

    <form action="index.php?action=update_metadata" method="POST">
    <input type="hidden" name="block_id" value="<?= htmlspecialchars($block['id']) ?>">
        
        <label for="alias">Alias / Nombre visible (R012):</label>
        <input type="text" id="alias" name="alias" value="<?= htmlspecialchars($block['alias']) ?>" placeholder="Asigna un nombre de proyecto más amigable" required>

        <div style="margin-bottom: 20px;">
    <label for="status">Estatus del Proyecto (R012):</label>
    <select name="status" id="status" style="width: 100%; padding: 10px;">
        <?php foreach ($available_statuses as $status): ?> <option value="<?= htmlspecialchars($status) ?>"
                    <?= $block['status'] === $status ? 'selected' : '' ?>>
                <?= ucfirst(str_replace('_', ' ', $status)) ?>
            </option>
        <?php endforeach; ?>
    </select>
</div>

<div style="margin-bottom: 20px;">
    <label for="tags">Etiquetas (separadas por comas, ej: MVP, Backend, UI):</label>
    <textarea name="tags" id="tags" rows="2" placeholder="Ej: Backend, SQL, API" required><?= htmlspecialchars($current_tags_string) ?></textarea>
</div>
        
        <button type="submit" style="background: var(--accent-color); margin-top: 20px;">Guardar Metadatos</button>
        <a href="index.php" style="display: block; text-align: center; margin-top: 10px; color: var(--primary-color);">Cancelar y Volver al Dashboard</a>
    </form>
</div>
</div>

<h3 style="margin-top: 40px; border-bottom: 2px solid #eee; padding-bottom: 10px; color: #7f8c8d;">
    <span style="color: #3498db;">G014</span> - Historial de Cambios (Bitácora)
</h3>

<?php if (empty($changelog)): ?>
    <p style="text-align: center; color: #95a5a6;">No hay registros de cambios para este proyecto inmutable.</p>
<?php else: ?>
    <div style="max-height: 400px; overflow-y: auto;">
        <?php foreach ($changelog as $log): ?>
            <div style="background: #fdf6e3; padding: 10px; border-left: 4px solid #f39c12; margin-bottom: 10px; border-radius: 4px; font-size: 0.9em;">
                <p style="margin: 0 0 5px 0; font-weight: bold;">
                    [<?= date('Y-m-d H:i:s', strtotime($log['created_at'])) ?>] por <?= htmlspecialchars($log['user_name']) ?>
                </p>
                <p style="margin: 0;">
                    Campo: <strong><?= ucfirst($log['field_name']) ?></strong>
                    <br>
                    De: <span style="color: #c0392b;">"<?= htmlspecialchars($log['old_value']) ?>"</span> 
                    A: <span style="color: #27ae60;">"<?= htmlspecialchars($log['new_value']) ?>"</span>
                </p>
            </div>
        <?php endforeach; ?>
    </div>
<?php endif; ?>

</body>
</html>