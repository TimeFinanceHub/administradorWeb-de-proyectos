<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>RG012 - Reportes</title>
    <style>
        :root {
            --primary-color: #2c3e50;
            --accent-color: #27ae60;
            --bg-color: #f4f4f9;
            --text-color: #333;
            --white: #ffffff;
            --red-color: #e74c3c;
            --blue-color: #3498db;
        }
        body { font-family: 'Courier New', monospace; background: var(--bg-color); color: var(--text-color); margin: 0; padding: 20px; line-height: 1.6; }
        .container { width: 100%; max-width: 900px; margin: 0 auto; background: var(--white); padding: 40px; border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); box-sizing: border-box; }
        header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #eee; padding-bottom: 20px; }
        h1 { font-size: 1.5rem; margin: 0; color: var(--primary-color); text-transform: uppercase; letter-spacing: 1px; }
        .stat-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-top: 20px; }
        .stat-card { background: #f9f9f9; padding: 20px; border-radius: 8px; text-align: center; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .stat-card h4 { margin-top: 0; font-size: 1.1rem; }
        .stat-card p { font-size: 2rem; margin: 5px 0; font-weight: bold; }
        .progress-bar-container { background: #ddd; border-radius: 5px; margin-top: 10px; }
        .progress-bar { height: 25px; background: var(--accent-color); border-radius: 5px; text-align: center; color: white; line-height: 25px; transition: width 0.5s; }
        .back-link { display: block; text-align: center; margin-top: 30px; color: var(--blue-color); text-decoration: none; font-weight: bold;}
    </style>
</head>
<body>

<div class="container">
    <header>
        <h1><span style="color: var(--blue-color);">G015</span> - Dashboard de Reportes</h1>
        <p>Análisis estadístico de la Cadena R012</p>
    </header>
    
    <a href="index.php" class="back-link">← Volver al Dashboard de Proyectos</a>

    <h2 style="border-bottom: 1px solid #eee; padding-bottom: 10px; margin-top: 40px;">✅ Progreso Global de Tareas</h2>
    <div style="background: #e6fff1; padding: 20px; border-radius: 8px;">
        <h4>Progreso: <?= $progress['progress'] ?>% Completado</h4>
        <p>Tareas Completadas: <?= $progress['completed'] ?> de <?= $progress['total'] ?></p>
        <div class="progress-bar-container">
            <div class="progress-bar" style="width: <?= $progress['progress'] ?>%;">
                <?= $progress['progress'] ?>%
            </div>
        </div>
    </div>

    <h2 style="border-bottom: 1px solid #eee; padding-bottom: 10px; margin-top: 40px;">🗂️ Proyectos por Estado</h2>
    <div class="stat-grid">
        <div class="stat-card" style="border-bottom: 4px solid var(--accent-color);">
            <h4>Activo</h4>
            <p style="color: var(--accent-color);"><?= $stats['active'] ?></p>
        </div>
        <div class="stat-card" style="border-bottom: 4px solid orange;">
            <h4>En Progreso</h4>
            <p style="color: orange;"><?= $stats['in_progress'] ?></p>
        </div>
        <div class="stat-card" style="border-bottom: 4px solid var(--blue-color);">
            <h4>Completado</h4>
            <p style="color: var(--blue-color);"><?= $stats['completed'] ?></p>
        </div>
        <div class="stat-card" style="border-bottom: 4px solid #7f8c8d;">
            <h4>Archivado</h4>
            <p style="color: #7f8c8d;"><?= $stats['archived'] ?></p>
        </div>
    </div>
    
</div>

</body>
</html>